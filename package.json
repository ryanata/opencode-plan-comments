{
  "name": "opencode-plan-comments",
  "version": "0.3.0",
  "type": "module",
  "license": "MIT",
  "description": "Inline comment plugin for opencode — leave feedback on agent plan output",
  "keywords": [
    "opencode",
    "plugin",
    "tui",
    "plan",
    "comments",
    "feedback"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ryanata/opencode-plan-comments.git"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./dist/index.js",
    "./tui": "./dist/tui.js"
  },
  "scripts": {
    "build": "rm -rf dist && NODE_ENV=production bun build src/tui.tsx src/index.ts --outdir dist --target bun --format esm --external @opencode-ai/plugin --external @opentui/core --external @opentui/solid --external solid-js",
    "prepublishOnly": "bun run build",
    "test": "bun test"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.4.0",
    "@opentui/core": ">=0.1.97",
    "@opentui/solid": ">=0.1.97",
    "solid-js": ">=1.9.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.4.3",
    "@opentui/core": "0.1.97",
    "@opentui/solid": "0.1.97",
    "solid-js": "1.9.10"
  }
}
